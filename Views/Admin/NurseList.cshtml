
@{
    ViewBag.Title = "Nurse List";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
    int co = 0;
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title"> Nurse List</h4>
                <div><a href="/Admin/Nurse" style="text-decoration:underline !important">Go Back</a></div>
                <br />
                <h6 class="card-subtitle">Export data to Copy, CSV, Excel, PDF & Print</h6>
                <div class="table-responsive m-t-30">
                    <table id="example23" class="display nowrap table table-hover table-striped table-bordered" cellspacing="0" width="100%">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Name</th>
                                <th>Mobile</th>
                                <th>Email</th>
                                <th>Gender</th>
                                <th>Address</th>
                                <th>Status</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tfoot>
                            <tr>
                                <th>Id</th>
                                <th>Name</th>
                                <th>Mobile</th>
                                <th>Email</th>
                                <th>Gender</th>
                                <th>Address</th>
                                <th>Status</th>
                                <th>Action</th>
                            </tr>
                        </tfoot>
                        <tbody>
                            @if (ViewBag.nurselist != null)
                            {
                                foreach (SanskarHospital.Models.NurseModel i in ViewBag.nurselist)
                                {
                                    <tr id="Busval_@i.nurse_id">

                                        <td>@(++co)</td>
                                        <td>@i.nurse_name</td>
                                        <td>@i.nurse_mobile</td>
                                        <td>@i.nurse_email</td>
                                        <td>@i.nurse_sex</td>
                                        <td>@i.nurse_address</td>

                                        @if (i.is_active == "ACTIVE")
                                        {
                                            <td><button class="btn btn-success btn-rounded">ACTIVE</button></td>
                                        }
                                        else
                                        {
                                            <td><button class="btn btn-danger btn-rounded">DEACTIVE</button></td>
                                        }

                                        <td class="text-nowrap">
                                            <a href="/Admin/Nurse/@i.nurse_id" data-toggle="tooltip" data-original-title="Edit"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>
                                            <a onclick="RemoveNurse(@i.nurse_id)" href="javascript:void(0)" data-toggle="tooltip" data-original-title="Remove"> <i class="fa fa-close text-danger"></i> </a>
                                        </td>

                                    </tr>
                                }
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>

    </div>
</div>

